var repeat = React.createClass({
    getInitialState: function () {
        return {
            items: ['One', 'Two', 'Three']
        };
    },
    render: function () {
        return repeatRT.apply(this);
    }
});