var <%= name %> = React.createClass({
    getInitialState: function () {
        return {
            items: ['One', 'Two', 'Three']
        };
    },
    render: function () {
        return <%= name %>RT.apply(this);
    }
});